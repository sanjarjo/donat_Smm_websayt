I found the actual problem.

The dashboard.js fetch is working correctly.

Console test:

fetch('/api/orders', {
  credentials: 'same-origin'
})
.then(r => {
  console.log('STATUS:', r.status);
  return r.text();
})
.then(console.log);

Result:

STATUS: 401

{"error":"Autentifikatsiya talab qilinadi."}

Also:

/api/user sometimes returns user data after login, but /api/orders returns 401.

So the issue is not missing credentials anymore.

Please investigate the backend session/auth middleware and the /api/orders route.

Things to check:

1. Is /api/orders using a different auth middleware than /api/user?
2. Is req.session.userId available inside /api/orders?
3. Is the session being regenerated after login and not saved correctly?
4. Are Render deployments using MemoryStore, causing sessions to disappear between requests?
5. Can you add console logs inside:
   - /api/login
   - auth middleware
   - /api/user
   - /api/orders

to compare req.session contents on each request?

The frontend is sending credentials correctly now, but /api/orders still receives an unauthenticated session and returns 401.
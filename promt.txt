Authentication is broken.

Facts already verified:

* connect.sid cookie exists.
* GET /api/user returns HTTP 200.
* GET /api/user returns {"user":null}.
* auth-ui.js depends on /api/user.
* Login appears successful but user state is lost after page reload.

Task:

1. Find the login endpoint.
2. Show the exact code that runs after successful login.
3. Show everything written into req.session.
4. Find GET /api/user.
5. Show exactly how GET /api/user determines the current user.
6. Compare login session fields vs /api/user session fields.
7. Identify the root cause.
8. Fix the bug.
9. Run authentication tests.
10. Verify that after login:

    * /api/user returns a real user object
    * Dashboard button appears
    * Login/Register buttons disappear
    * Refresh keeps the user logged in

Output:

* file names
* line numbers
* code changes
* test results
